---
- name: Setup | Setup playbook
  hosts: all
  tasks:
  - name: Setup | Set facts
    include_tasks: set_facts.yml

  - name: Setup | Load tasks
    include_tasks: "{{ item }}.yml"
    with_items:
      - packages
      - bash
      - git
      - nvim
      - tmux
      - bin_scripts


### packages:
install packages from yml for system if yml exists

```
- type of package (e.g apt-get):
  - package1
  - package1
- other type (e.g. pip):
  - ansible
  - foo
```


### README
update that
